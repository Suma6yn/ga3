name: Deploy Flask Todo App to AWS EC2

on:
  workflow_dispatch:
    
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r flask-bootstrap-app/requirements.txt

      - name: Create .env file from secrets
        run: |
          echo "MYSQL_HOST=${{ secrets.MYSQL_HOST }}" >> flask-bootstrap-app/.env
          echo "MYSQL_USER=${{ secrets.MYSQL_USER }}" >> flask-bootstrap-app/.env
          echo "MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}" >> flask-bootstrap-app/.env
          echo "MYSQL_DB=${{ secrets.MYSQL_DB }}" >> flask-bootstrap-app/.env
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" >> flask-bootstrap-app/.env
          echo "AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY_ID }}" >> flask-bootstrap-app/.env
          echo "AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> flask-bootstrap-app/.env

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~
            git pull origin main
            python3 -m pip install -r flask-bootstrap-app/requirements.txt
            export $(cat flask-bootstrap-app/.env | xargs)
            python3 flask-bootstrap-app/app.py &
